'use client';

import { Dashboard } from '@/components/Dashboard';

export default function DashboardPage() {
  return (
    <main id="main">
      <Dashboard onNavigate={() => {}} />
    </main>
  );
}
